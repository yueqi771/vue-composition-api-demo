import axios from '.'

export function getSliders<T>() {
    return new Promise((resolve) => {
        setTimeout(() => {
            resolve([
                 
            ])
        }, 2000)
    })
}
